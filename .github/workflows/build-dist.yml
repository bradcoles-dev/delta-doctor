name: Build dist notebooks

on:
  push:
    branches: [main]
    paths:
      - 'Notebooks/**/notebook-content.py'
      - 'scripts/build_notebooks.py'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Build dist/ from notebook-content.py sources
        run: python scripts/build_notebooks.py

      - name: Commit updated dist/ if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/
          git diff --staged --quiet || git commit -m "Rebuild dist/ notebooks from source [skip ci]"
          git push
